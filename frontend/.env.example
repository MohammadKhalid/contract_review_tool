# Frontend Environment Configuration
# Copy this file to .env.local and fill in your values

# Backend API URL (only public, non-sensitive variable)
NEXT_PUBLIC_API_BASE_URL=http://localhost:5001

# NOTE: NEVER put database passwords, API keys, or any secrets in this file.
# Those belong in the backend .env file only.
# The frontend should never have access to database credentials.