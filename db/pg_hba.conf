# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
# Changed from "peer" to "scram-sha-256" so that connections as DB user "user"
# can succeed even if the connecting process runs as a different OS user (e.g. root).
# Password must be provided via PGPASSWORD env var.
local   all             all                                     scram-sha-256

# IPv4 local connections:
host    all             all             127.0.0.1/32            scram-sha-256

# IPv6 local connections:
host    all             all             ::1/128                 scram-sha-256

# Allow connections coming through the published port (SSH tunnel from pgAdmin).
# These appear to Postgres as coming from the host's public IP (e.g. 178.105.225.106).
# We deliberately use "host" (plain, no SSL) here because the SSH tunnel provides the encryption.
# Password authentication (scram-sha-256) is still enforced.
host    all             all             0.0.0.0/0               scram-sha-256
host    all             all             ::/0                    scram-sha-256

# Allow replication connections from localhost, by a user with the
# replication privilege.
local   replication     all                                     peer
host    replication     all             127.0.0.1/32            scram-sha-256
host    replication     all             ::1/128                 scram-sha-256
